UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""

LASTFM_API_KEY=""
LASTFM_USERNAME=""

# Password for the /shortener admin page. The raw value is only kept in env;
# session cookies are HMAC-signed with a key derived from this password,
# so rotating it invalidates every existing session.
SHORTENER_PASSWORD=""
const path = require('path')

module.exports = {
  i18n: {
    defaultLocale: 'en',
    locales: ['en', 'hn', 'de', 'fr', 'sr'],
    ignoreRoutes: ['/blog/', '/public/'],
    serializeConfig: false,
    defaultNS: 'app',
    serializeConfig: false,
  },
}

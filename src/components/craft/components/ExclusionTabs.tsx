'use client'

import Post from '../Post'

export default () => {
  return (
    <Post
      title='Exclusion Tabs'
      description="Filters from Stripe's blog using clip-path."
      tags={['react', 'tailwindcss', 'waapi']}
    >
      <div></div>
    </Post>
  )
}
